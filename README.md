# A sample project to deploy some spring projects by using docker
To run the project: In the api-gateway directory,

    make run

And then go to http://localhost:80, then you will see the result page.
